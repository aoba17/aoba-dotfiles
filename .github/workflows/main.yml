---
name: main

on: [push]

jobs:
  main:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v1
      - name: install
        run: bash install.sh
      - name: load .zshrc
        shell: zsh {0}
        run: source ~/.zshrc
      - name: test eza command
        shell: zsh {0}
        run: la